# Architecture Notes

This directory contains lightweight architecture notes for TimbrosaField.

The current structure keeps runtime code under `src/my_app`, tests under `tests`, data-maintenance tools under `scripts/data_tools`, and static application resources inside `src/my_app/resources` so they can be loaded consistently during development and packaged builds.

Historical refactor logs and machine-local audit notes are intentionally not kept in the public repository.
